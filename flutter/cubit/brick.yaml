name: cubit
description: A new brick created with the Mason CLI.
vars:
  - name
  - is_hydrated
  - path
  # - dependencies
  # dependencies = {
  # n: <name of dependency>
  # isRepo: <boolean>
  # }
